function getBreadboardPinOffset(pinIdx) {  let x = 0, z = 0;  if (pinIdx < 150) {    let row = Math.floor(pinIdx / 5);    let col = pinIdx % 5;    x = -7.0 + row * 0.5;    z = -1.5 + col * 0.5;  } else if (pinIdx < 300) {    let row = Math.floor((pinIdx - 150) / 5);    let col = (pinIdx - 150) % 5;    x = -7.0 + row * 0.5;    z = 1.5 + col * 0.5;  } else if (pinIdx < 330) {    let idx = pinIdx - 300;    x = -7.0 + idx * 0.5;    z = -3.0;  } else if (pinIdx < 360) {    let idx = pinIdx - 330;    x = -7.0 + idx * 0.5;    z = -2.5;  } else if (pinIdx < 390) {    let idx = pinIdx - 360;    x = -7.0 + idx * 0.5;    z = 4.5;   } else if (pinIdx < 420) {    let idx = pinIdx - 390;    x = -7.0 + idx * 0.5;    z = 5.0;  }  return [x, 0, z];}
